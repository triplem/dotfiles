#+title: Various graphs

These come from https://matplotlib.org/stable/gallery/index.html.

* Bar graph

#+BEGIN_SRC jupyter-python
import matplotlib.pyplot as plt

fig, ax = plt.subplots()

fruits = ['apple', 'blueberry', 'cherry', 'orange']
counts = [40, 100, 30, 55]
bar_labels = ['red', 'blue', '_red', 'orange']
bar_colors = ['tab:red', 'tab:blue', 'tab:red', 'tab:orange']

ax.bar(fruits, counts, label=bar_labels, color=bar_colors)

ax.set_ylabel('fruit supply')
ax.set_title('Fruit supply by kind and color')
ax.legend(title='Fruit color');
#+END_SRC

#+RESULTS:
[[file:./.ob-jupyter/703c8d7e9e2fbd85b4166cb0837a756238320d2d.png]]

* Contour plot

#+BEGIN_SRC jupyter-python
import matplotlib.pyplot as plt
import numpy as np

t = np.linspace(0, 2 * np.pi, 1024)
data2d = np.sin(t)[:, np.newaxis] * np.cos(t)[np.newaxis, :]

fig, ax = plt.subplots()
im = ax.imshow(data2d)
ax.set_title('Pan on the colorbar to shift the color mapping\n'
             'Zoom on the colorbar to scale the color mapping')

fig.colorbar(im, ax=ax, label='Interactive colorbar');
#+END_SRC

#+RESULTS:
[[file:./.ob-jupyter/be52ee39ac3e904b269539bedcebefc6d5debc74.png]]

* Quiver plot

#+BEGIN_SRC jupyter-python
import matplotlib.pyplot as plt
import numpy as np

X = np.arange(-10, 10, 1)
Y = np.arange(-10, 10, 1)
U, V = np.meshgrid(X, Y)

fig, ax = plt.subplots()
q = ax.quiver(X, Y, U, V)
ax.quiverkey(q, X=0.3, Y=1.1, U=10,
             label='Quiver key, length = 10', labelpos='E');
#+END_SRC

#+RESULTS:
[[file:./.ob-jupyter/e279d56c12caec82a81196932c7a76a43408fb07.png]]
